# Frontend

React interface for the synthetic social agent chat prototype.

```bash
npm install
npm start
```

The app loads 30 cached persona profiles from the backend and opens each profile in a popup chat window.
