services:
  subjectivity-classifier:
    platform: linux/amd64
    build:
      context: ./services/subjectivity_classifier
    ports:
      - "8001:8001"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8001/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
